#include <iostream>
#include "window/window.hpp"

int main(int argc, char const *argv[])
{
    // Snake sk;
    // Food fd;
    Window window(30, 30);
    window.initWindow();
    //    mat.initBoard();
    //    mat.drawBoard(sk,fd);
    return 0;
}
